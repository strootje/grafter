import { Changeable } from '../../Changeable';
import { SourceAttributes } from '../../Sourceable';

export class Generic extends Changeable {
	protected ParseMatter(_data: SourceAttributes): void {
	}
}
